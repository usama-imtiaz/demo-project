<%= form_with model: [@product, @comment], remote: true do |f| %>
  <div class="form-group">
    <%# <%= f.label :comment, class: "col-sm-2 control-label" %>
        <%= f.text_field :comment, type: "", class: "form-control", id: "text-check"%>
  </div>
<% end %>