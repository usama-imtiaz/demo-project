$("#comments_section").html("<%= j render(@comments) %>");
$("#comment-<%= @comment.id %>").replaceWith("<%= j render 'form', product: @product, comment: @comment %>");
$("#comments_section #text-check").focus();
