<div>
  <% if user_signed_in? %>
    <%= content_tag :div, id: 'product', data: {prod_id: product.id, user_id: current_user.id } do %>
    <% end %>
  <% else %>
    <%= content_tag :div, id: 'product', data: {prod_id: product.id } do %>
    <% end %>
  <% end %>
  <button type="submit" class="add_to_cart btn-sm btn-primary mt-2 p-2" style="width: 100%;">Add to Cart</button>
</div>
